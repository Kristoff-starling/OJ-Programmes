#include <bits/stdc++.h>
#define rep(i,a,b) for (int i=a;i<=b;i++)
using namespace std;

int main ()
{
    int ca;scanf("%d",&ca);
    double p,q;
    while (ca--)
    {
        scanf("%lf%lf",&p,&q);
        if (p>q) puts("ENJ0Y YOURS3LF!"); else puts("N0 M0R3 BL4CK 1CE TEA!");
    }
    return 0;
}