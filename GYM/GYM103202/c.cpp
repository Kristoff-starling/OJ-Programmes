#include <bits/stdc++.h>
#define rep(i,a,b) for (int i=a;i<=b;i++)
using namespace std;

const int MAXN=1e6;

int n,m;
int T[MAXN+48],F[MAXN+48];

char s[MAXN+48];

int main ()
{
    scanf("%d%d",&n,&m);
    rep(i,1,n)
    {
        scanf("%[^\n]",s+1);
    }
}