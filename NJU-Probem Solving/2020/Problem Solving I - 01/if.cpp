#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int number;scanf("%d",&number);
    if (number==0) puts("zero");
    if (number==1) puts("one");
    if (number==2) puts("two");
    if (number==3) puts("three");
    if (number==4) puts("four");
    return 0;
}