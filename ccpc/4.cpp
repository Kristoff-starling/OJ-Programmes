#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int ca;scanf("%d",&ca);
    long long x;
    while (ca--)
    {
        scanf("%lld",&x);
        if (x==1) puts("YES"); else puts("NO");
    }
    return 0;
}